#include <stdio.h>
int main()
{
    char name[100];
    double salary,sales, TOTAL;
    scanf("%s %lf %lf",name,&salary,&sales);
    TOTAL = salary + sales*0.15;
    printf("TOTAL = R$ %.2lf\n", TOTAL);
    return 0;
}
