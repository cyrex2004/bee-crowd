#include <stdio.h>
int main()
{
    int i,n,multiplication;
    scanf("%d",&n);

    if(n>1 && n<1000)
    {
        for(i=1;i<=10;i++)
        {
            multiplication = i*n;
            printf("%d x %d = %d\n",i,n,multiplication);
        }
    }


    return 0;
}
