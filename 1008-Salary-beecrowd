#include <stdio.h>
int main()
{
    int number,hour;
    float salary_rate,salary;
    scanf("%d %d %f", &number,&hour,&salary_rate);
    printf("NUMBER = %d\n",number);
    salary = hour*salary_rate;
    printf("SALARY = U$ %.2f\n",salary);
    return 0;
}
