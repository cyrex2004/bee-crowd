#include <stdio.h>

int main() {
    int numbers[5];

    int even_count = 0;


    for (int i = 0; i < 5; i++) {
        scanf("%d", &numbers[i]);
        if (numbers[i] % 2 == 0) {
            even_count++;

        }
    }

    printf("%d valores pares\n", even_count);

    return 0;
}
